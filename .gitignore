# Terraform local state / cache
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log

# .terraform.lock.hcl은 Provider 재현성을 위해 Git에 커밋하는 것을 권장한다.

# 실제 계정/이메일/IP 등 로컬 변수
terraform.tfvars
*.auto.tfvars

# OS / Editor
.DS_Store
.vscode/
.idea/
